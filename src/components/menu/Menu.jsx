import React from "react";
import styles from './menu.module.css'
const Menu = () => {
    return (
        <div className={`${styles.container} ${styles.menu}`}> Menu</div>
    );
}

export default Menu